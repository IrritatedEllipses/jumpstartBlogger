<h1>All Tags</h1>

<ul id="tags">
  <% @tag.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag) %>
    </li>
    <li>
    <%= link_to "delete", [tag], method: :delete, data: {confirm: "Really delete the tag?"}
%>
  <% end %>
</ul>